# This is the Samizdat manifest file for this project.

[series]
# Public information of the series in this directory. The nickname is a
# NODE-LOCAL alias; it only identifies this series-owner inside your own
# samizdat-node and has no meaning on the network. What other peers see is
# only the public-key below.

nickname = "{{ nickname }}"
public-key = "{{ public_key }}"
# ttl = "{{ ttl }}"    # set a time-to-live different from the default


[debug]
# Same thing as the last section, but for debugging and testing purposes.
# `samizdat commit` defaults to publishing under THIS nickname; the public
# `[series]` above is only published when you pass `--release`.

nickname = "{{ nickname }}-debug"
# ttl = "{{ ttl }}"


[build]
# Build instructions for this series.

base = "./dist" # the input directory that Samizdat will read from
# run = "npm run build" # a build command to be run before upload
